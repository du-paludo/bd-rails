<h1>All Users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Admin</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.admin? ? 'Yes' : 'No' %></td>
      </tr>
    <% end %>
  </tbody>
</table>