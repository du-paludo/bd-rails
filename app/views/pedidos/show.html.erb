<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">

<body>
    <main>
        <p style="color: green"><%= notice %></p>
        <%= render @pedido %>
        <p>
            <%= link_to "Voltar para pedidos", pedidos_path %>
            <% if current_user.admin? %>
                | <%= link_to "Editar pedido", edit_pedido_path(@pedido) %>
            <% end %>
        </p>
         <% if current_user.admin? %>
            <p><div class="delete_button"><%= button_to "Excluir pedido", @pedido, method: :delete, form_class: "button", data: { confirm: 'Você tem certeza que deseja deletar essa entrada?' } %></div></p>
        <% end %>
    </main>
</body>